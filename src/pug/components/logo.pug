mixin logo(type = '', size='medium')
  |
  |
  span.logo-main(class=type+' '+size)
    |
    img(src=assets + brand.medical.img alt='logo')/
    | #{type !== 'only' ? brand.medical.name : ''}
  |
  |
